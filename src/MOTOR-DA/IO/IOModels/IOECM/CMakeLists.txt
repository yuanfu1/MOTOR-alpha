cmake_minimum_required(VERSION 3.5)

PROJECT(IOECM)
INCLUDE_DIRECTORIES($ENV{COMMON_MODS})
LINK_DIRECTORIES($ENV{COMMON_LIBS})

include (srcFiles.txt)

ADD_LIBRARY(IOECM STATIC ${IOECM_SRC})
TARGET_LINK_LIBRARIES(IOECM PUBLIC Utility MPDD State IOModel YAMLRead InterpHP ModelCoupler M2CV ${MPI_Fortran_LIBRARIES} YAMLRead MGOpts)

#SET (EXE2 "Test_IOECM.exe")
#ADD_EXECUTABLE(${EXE2} Test_IOECM.F90)
# TARGET_LINK_LIBRARIES(${EXE2} Utility IOECM GrapesIO MPDD Geometry State State2NC ${MPI_Fortran_LIBRARIES} MGOpts IOGrapes)

